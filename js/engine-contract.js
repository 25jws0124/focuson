/* =====================================================================
 * FocusOn — engine-contract.js
 *
 * 엔진과 화면이 함께 쓰는 값. 여기가 유일한 출처다.
 *
 * 왜 별도 파일인가:
 *   app.js 가 focus-engine.js 를 직접 import 하면 MediaPipe 번들까지
 *   따라 들어와서, 카메라·모델 없이 UI만 보는 ?mock 모드의 의미가 없어진다.
 *   그래서 의존성이 전혀 없는 이 파일에 공유 값만 둔다.
 * ===================================================================== */

/** 상태 판정 경계. 화면의 '집중 기준선'도 이 값을 그린다. */
export const THRESHOLDS = {
  focused: 66,     // 이 이상이면 집중 후보
  distracted: 54,  // 이 미만이면 딴짓 후보
};
